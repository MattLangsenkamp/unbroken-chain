[project]
name = "unbroken-chain-docs"
version = "0"
description = "MkDocs harness for the Unbroken Chain documentation site."
requires-python = ">=3.12"
dependencies = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.5,<10",
    "mkdocs-monorepo-plugin>=1.1,<2",
]

[tool.uv]
package = false
